version: '3.8'

services:
  aiemail-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aiemail-container
    ports:
      - "5174:80"
    environment:
      - VITE_API_BASE_URL=http://localhost:8080
